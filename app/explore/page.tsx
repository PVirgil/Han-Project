"use client"; import {useState} from "react"; import {featured} from "@/lib/data";
export default function Explore(){const [q,setQ]=useState("");const [answer,setAnswer]=useState("");const [busy,setBusy]=useState(false);
async function ask(e:React.FormEvent){e.preventDefault();setBusy(true);setAnswer("");try{const r=await fetch("/api/ask",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({question:q})});const j=await r.json();setAnswer(j.answer||j.error)}catch{setAnswer("The research assistant could not be reached.")}finally{setBusy(false)}}
return <><section className="pageHead"><div className="eyebrow">RESEARCH WORKSPACE</div><h1>Civilization Explorer</h1><p>Start with an entity or ask a historical question.</p></section><div className="workspace"><aside><h3>Entities</h3>{featured.map(x=><button className="entityButton" onClick={()=>setQ(`Explain ${x.title} (${x.ko}) in historical context and identify the kinds of primary sources a researcher should consult.`)} key={x.title}>{x.title}<small>{x.ko} · {x.type}</small></button>)}</aside><section className="ask"><h2>Ask HAN</h2><form onSubmit={ask}><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Why did Joseon adopt Neo-Confucian state institutions?" required/><button className="button" disabled={busy}>{busy?"Researching…":"Investigate"}</button></form>{answer&&<div className="answer"><h3>Research note</h3><p>{answer}</p></div>}<div className="notice">AI output is a research aid, not a primary source. Verify important claims against cited scholarship and archival material.</div></section></div></>}
