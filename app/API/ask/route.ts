import OpenAI from "openai";
export const runtime="nodejs";
export async function POST(req:Request){try{const {question}=await req.json();if(!question||typeof question!=="string")return Response.json({error:"Question required."},{status:400});if(!process.env.OPENAI_API_KEY)return Response.json({answer:"AI is not configured yet. Add OPENAI_API_KEY in Vercel Environment Variables. The rest of HAN works without it."});
const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const r=await client.responses.create({model:process.env.OPENAI_MODEL||"gpt-5-mini",instructions:"You are HAN, a careful research assistant for Korean civilization. Be concise, distinguish established facts from interpretation, mention uncertainty, preserve Korean names where useful, and recommend categories of primary and scholarly sources. Never invent citations.",input:question});return Response.json({answer:r.output_text});}catch(e){console.error(e);return Response.json({error:"Research request failed."},{status:500})}}
